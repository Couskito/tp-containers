apiVersion: v1
kind: Pod
metadata:
  name: ratings
  labels:
    app: ratings
spec:
  containers:
    - name: ratings
      image: localhost:5001/ratings:1.0
      ports:
        - containerPort: 9080
      env:
        - name: SERVICE_VERSION
          value: "v1"
